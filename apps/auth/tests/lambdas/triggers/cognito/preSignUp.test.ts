describe('auth/lambdas/triggers/cognito/preSignUp', () => {
  it('it should work', () => {
    // eslint-disable-next-line no-console
    console.log("Hey don't forget to implement tests!");
  });
});
